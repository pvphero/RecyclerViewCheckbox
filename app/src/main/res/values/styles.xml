<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/bind_title</item>
        <item name="colorPrimaryDark">@color/bind_title</item>
        <item name="colorAccent">@color/bind_title</item>
    </style>

</resources>
